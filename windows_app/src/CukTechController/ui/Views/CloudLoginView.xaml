<UserControl x:Class="CukTechController.Views.CloudLoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="米家云登录"
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="10"/>

        <wv2:WebView2 Grid.Row="1" x:Name="LoginWebView">
            <wv2:WebView2.Source>
                <Uri>https://account.xiaomi.com/pass/serviceLogin?sid=xiaomiio&amp;_json=True</Uri>
            </wv2:WebView2.Source>
        </wv2:WebView2>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Center" Margin="10">
            <Button Content="刷新" Click="RefreshButton_Click" Margin="5"/>
            <Button Content="取消" Click="CancelButton_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
